package com.unitx.signal_core.contract.position

/**
 * Screen edge where the snackbar appears.
 * Configured via [SnackConfig.position].
 */
enum class SnackPosition {
    /** Snackbar slides in from the top of the screen. */
    Top,
    /** Snackbar slides in from the bottom of the screen. */
    Bottom
}